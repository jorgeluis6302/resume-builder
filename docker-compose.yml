version: '3.8'

networks:
  default:

services:
  db:
    image: postgres:latest
    container_name: res-builder-db
    ports:
      - 5432:5432
    volumes:
      - "./.data/db:/var/lib/postgresql/data"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: 6marzo1902